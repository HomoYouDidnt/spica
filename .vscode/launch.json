{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "SPICA Demo (cells/local.yaml)",
      "type": "python",
      "request": "launch",
      "module": "spica.demo",
      "args": ["--cells", "configs/cells/local.yaml"],
      "console": "integratedTerminal",
      "env": {
        "PYTHONUTF8": "1",
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
}

